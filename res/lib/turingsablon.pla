/* ----------------------------------------------
		Turing-gép
-------------------------------------------------*/
#pragma turing

term a, b;

void main()
{
	input(a a b b);
	// Írja ide az utasításokat
	accept;
}