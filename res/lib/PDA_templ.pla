/* ----------------------------------------------
		Pushdown Automaton
-------------------------------------------------*/
#pragma pushdown

start Z;
term a, b;

void main()
{
	input(a a b b);
	// Write here the commands...
}